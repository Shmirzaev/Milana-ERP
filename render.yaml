databases:
  - name: milana-erp-db
    databaseName: milana_erp
    user: milana_erp

services:
  - type: web
    name: milana-erp
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: milana-erp-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_ALGORITHM
        value: HS256
      - key: JWT_EXPIRES_MINUTES
        value: "1440"
      - key: INITIAL_ADMIN_EMAIL
        value: admin@example.com
      - key: INITIAL_ADMIN_PASSWORD
        sync: false
      - key: SEED_DEMO_USERS
        value: "false"
      - key: RUN_SEED_ON_STARTUP
        value: "true"
      - key: APP_NAME
        value: Milana ERP
      - key: DEBUG
        value: "false"
      - key: CORS_ORIGINS
        value: "*"
      - key: BARCODE_STORAGE_DIR
        value: /opt/render/project/src/backend/storage/barcodes
      - key: SALES_ORDER_FILES_DIR
        value: /opt/render/project/src/backend/storage/sales_order_files

  - type: web
    name: milanaerp-frontend
    runtime: docker
    plan: free
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: .
    healthCheckPath: /login
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: milana-erp
          envVarKey: RENDER_EXTERNAL_URL
      - key: NODE_ENV
        value: production
