# Stabilization QA

Scope: `feat/ismoiljon`, PR #175. **Local synthetic data only. Never use production URLs, credentials, dumps or devices.** Main findings: [audit](../audit_ismail.md); complete baseline: [backlog](audit_backlog.json).

## Run

Install the pinned backend requirements and frontend lockfile dependencies in a disposable checkout. Commands below start at the repository root. Backend fixtures create and reset a temporary SQLite database; concurrency tests need separate PostgreSQL.

```text
cd backend
python -m pytest -q
cd ..
python -m pytest -q connectors/hikvision_attendance
bun run --cwd frontend test:stabilization
bun run --cwd frontend lint
bun run --cwd frontend typecheck:strict
bun run --cwd frontend build
python scripts/run_isolated_postgres_tests.py --pg-bin "PATH/TO/POSTGRES/bin" -q -k postgres
```

The PostgreSQL launcher creates a fresh loopback-only cluster on an unused port, supplies test-only credentials, and stops it in `finally`. It does not connect to the installed service. Diagnostic files remain in its printed temporary directory. Run database-heavy suites one at a time on memory-limited laptops.

CI portability: `94972e3` failed15 packaging cases on CI SQLite; PostgreSQL/frontend passed. `e1b3b78` removes outer references from subquery ordering while preserving exact-batch/legacy/oldest precedence. Rerun `test_packaging_receive_options_query_growth.py` on both engines and the packaging-receive API workflow; local reruns pass, combined CI remains required.

Final cycle validation: run `35593521935` on application revision `dafa169` passed the backend suite, frontend lint/strict types/build/contracts, and all 115 selected PostgreSQL cases; release skipped. The first PostgreSQL attempt was 114 passed/1 failed because `test_postgres_package_reserve_and_damage_keep_one_valid_state` observed the valid damage-first rejection 400 while asserting only 409. One failed-job rerun passed. Treat the nondeterministic assertion as a separate test-contract defect; do not erase the first attempt.

Second fix batch: the Cutting, catalog rename and thumbnail regressions pass together locally (**42 passed**), and changed backend files pass Ruff plus Python compilation. `check-model-seasons-hidden-fetch.mjs` passes. Validation-only run `35672476252` on `8589ba4` passed backend, frontend lint/strict types/optimized build/contracts, and all 115 PostgreSQL selections; release skipped. First run `35672321116` failed the known nondeterministic reserve/damage assertion with a valid 400 instead of expected 409 and found the new script's mock `useSWR` name violated hook lint; `8589ba4` renamed the mock and the corrected run passed both jobs.

Third fix batch: corrected Sewing conservation, catalog approval batching and the payroll cap pass together locally (**33 passed**); changed backend files pass Ruff plus Python compilation. The four production-flow cases that exposed Sewing loss semantics pass within a 24-test compatibility selection. Both model-detail hidden-fetch contracts pass, including read-only BOM metadata on the visible Materials tab. Validation-only run `35675969462` on `bae444d` passed backend, frontend lint/strict types/optimized build/contracts and all PostgreSQL selections; release skipped. The preceding run `35674868826` passed frontend/PostgreSQL but failed those four backend cases before correction.

Continuation batch: the main backend regression slice passed **173 tests / 13 PostgreSQL-only skips**; the later cross-area finance/dashboard/HR/catalog/packaging/Finished Goods/accessory/sales slice passed **145 / 8 skips**. Focused follow-ups include reservation/stocktake **27 / 10 skips**, receive queue **8**, JWT startup **5**, HR workspace/validation **35**, packaging search **17 / 1 skip**, and Finished Goods bounds **2**. `npm run test:stabilization` now executes the new package-recovery, pricing/Usluga pagination, cutting-passport, HR calendar/positions and Finished Goods component/hook regressions; it passes with strict TypeScript and targeted ESLint. These tests use synthetic local data and do not close the umbrella partial findings or provide production/load sign-off.

For one regression: `cd backend`, then `python -m pytest -q app/tests/<file>.py`. Add `-s` to see query counts. Do not set a PostgreSQL URL manually unless it points to a verified disposable database.

## Acceptance cases

Each row names a repeatable regression. Browser checks complement these; they do not prove database concurrency.

| Bug | Test / where | Expected result |
| --- | --- | --- |
| DB07 identifier expansion | `test_compact_order_numbers.py`, `test_canonical_bundle_migration.py`, `test_canonical_bundle_qr.py`, `test_numbering_postgres_concurrency.py` |22 compatibility cases pass. Disposable PostgreSQL proves two blocked writers cross9999 uniquely. Historical migrations unchanged. |
| PY04 payroll date match | `test_payroll_period_matching.py`; single/bulk record creation |Matching dates auto-attach; unmatched dates stay periodless; explicit manual period remains.2 focused cases independently pass; broader agent run52 passed/6 PostgreSQL skipped. |
| PERF09 package writes (fixed) | `test_package_write_query_growth.py`, `test_package_write_postgres.py`, `test_package_bulk_cost_reuse.py`, `test_package_batch_allocation_query_growth.py`, workflow/numbering/cost suites |At1/50/401: availability3/3/3, numbering2/2/2, notification directory2/2/2, workflow/presence/membership/owner1/1/1 and print children2/2/4; allocations2/2/3 and costing2/2/2 remain bounded. Thirty-four focused +34 adjacent +2 real-PG lock/rollback cases pass. Scalar/bulk lock hierarchy, rollback number reuse, ordering, factory ownership, weights/items and validation parity hold. Required per-row writes/output remain O(N). |
| PERF39 recruitment (fixed slice) | `frontend/scripts/check-hr-recruitment-hidden-fetch.mjs`; HR → Recruitment |Closed page skips departments; Add candidate and View/edit load/render them. Visible recruitment/position requests remain. Actual component test independently passes; types/lint pass. |
| PERF26 reservation and metadata repair (fixed) | `test_sales_branded_variant_queries.py`, `test_sales_branded_candidate_cache.py`, `test_finished_goods_brand_metadata_query_growth.py`, `test_sales_branded_stock.py`, `test_sales_ready_packs.py`, `test_legacy_finished_goods.py`, `test_manual_finished_goods.py` |Full reservation1/50/401 variants: total SELECTs3/52/403→3/3/5; stock SELECTs2/51/402→2/2/4 in200-key chunks. Distinct missing-metadata rows use1/1/2 sales-item and1/1/2 collection reads with one stock read at1/50/401. Fifteen focused and99 adjacent cases pass. Package-first locks, wildcard/brand filters, shortage, metadata repair, legacy receipt and exact/whole-pack integrations remain compatible; mutable stock eligibility is never cached. Necessary per-row metadata updates, reservations and output remain O(N). |
| PERF32 inbox/forecast references (fixed) | `test_inbox_production_context_query_growth.py`, `test_forecasting_reference_query_growth.py`, `test_traceability_forecasting.py` |At1/50/401, material payloads use4/4/8 SELECTs, planned BOM demand4/4/7, production context6/6/6, branded analysis7/7/10 and dashboard15/15/18. SQL projections omit image BLOBs, composition JSON, production attachments and stock-batch roll/image payloads while preserving URL/fallback precedence. Forty-four focused/adjacent cases pass; reference trips are400-ID chunk-bounded and required output work remains O(N). |
| PERF39 hidden fetch (fixed slice) | `frontend/scripts/check-hr-document-hidden-fetch.mjs`; HR → Documents |Closed upload modal does not request employees; opening it requests and displays options. Actual component test independently passes; strict types/lint pass. |
| PERF39 inventory hidden fetch (fixed slice) | `frontend/scripts/check-inventory-master-hidden-fetch.mjs`; Inventory → Master data |Materials skips the Accessories directory; opening Accessories requests the same authorized endpoint and renders its data. Stabilization suite, strict types and lint pass. |
| PERF39 purchasing hidden fetch (fixed slice) | `frontend/scripts/check-purchasing-request-hidden-fetch.mjs`; Purchasing |Closed Create request skips two500-item directories; opening it requests the same authorized endpoints and renders options. Stabilization, strict types and lint pass. |
| PERF39 model seasons/BOM (fixed slice) | `frontend/scripts/check-model-seasons-hidden-fetch.mjs`, `check-model-bom-hidden-fetch.mjs`; Model detail |Read-only detail skips the seasons directory while retaining the saved season. Hidden tabs skip BOM metadata; the visible Materials tab loads names/costs in read and edit modes. Both source/behavior contracts pass; later full stabilization, strict types and lint are green. |
| WF08 sale UI (partial) | `frontend/scripts/check-waste-sale-recovery.mjs`; Waste → Sell |Enter real buyer, positive quantity and explicit nonnegative price; current remaining stock is displayed. Lost response/reload reconciles the exact key without replaying the sale. localStorage, Web Locks and storage events coordinate tabs; corrupt/conflicting evidence and missing locks fail closed, and originating-tab sessionStorage migrates. The actual handler/helper contract and independent rerun pass. Visual/mobile QA and unopened-tab legacy migration remain outside this gate. |
| PERF13 bundle receipt (fixed) | `test_bundle_receive_query_growth.py`, `test_bundle_accessory_gate_batching.py`, `test_bundle_sewing_batch_aggregate_projection.py`; manual/batch sewing acceptance |26 focused cases pass. Preserve scalar state/logs, NULL/multiple batches, mixed routes, rollback and invalid gates. At1/50/401 bundles, manual receipt uses20 SELECTs and acceptance29 at every size; accepted response aggregation reuses loaded rows. Required state/scans/audits remain O(N) writes, but repeated lookup/gate reads are resolved. |
| PERF27 shipment locks (partial) | `test_shipment_package_lock_query_growth.py` plus two successful shipment-review workflows |Seven independently passing cases. One Package SELECT at1/50/401 in isolated component tests; reused sorted locks refresh stale identities. Reject invalid middle package; real stock/dispatch/delivery workflows stay correct. Other queries/sync remain per package; no total O(1) claim. |
| PERF10 batch storage (fixed) | `test_package_batch_storage_query_growth.py`, `test_package_batch_receive_concurrency.py`; batch receive/place and print-run receive |Direct batch receive/place retains12/12/23 SELECTs at1/50/401. Full print-run receive falls21/756/6021→18/18/31 by reusing one locked gate and batch-loading children, members, source types, stock and response references in manifest order. Twenty-three focused SQLite and3 real-PostgreSQL concurrency/rollback cases pass. Per-package state, scan and audit writes remain necessary O(N). |
| PERF18 receive options (fixed) | `test_packaging_receive_options_query_growth.py`, `test_sewing_receive_option_pagination.py`; packaging/sewing receive-options |Packaging uses2 SELECTs and sewing5 at1/50/401 with bounded SQL search/limits. PostgreSQL materializes exact/fallback packaging target maps; all6 real-PG plans retain top-level limits with no subplans or aggregate loops equal to scope count. Twenty-five focused cases pass with6 expected local PG skips. Auth, factory scope, exact/legacy/oldest precedence, search/department parity and no-write behavior remain covered. |
| OPS08 dependency patches (partial) | `test_dependency_lifecycle_contract.py`, `test_auth_credential_cutoff.py`, `test_startup_schema.py`; image/upload/QR regressions; frontend stabilization/types/build and package audits | Pillow12.3 and Next16.3.5/PostCSS8.5.23 remain clean. `06705b9` replaces python-jose/ecdsa with PyJWT2.14.0 and Node20 with Node24 LTS. Focused19 and broad85 security cases pass; fresh pip-audit and npm full/production audits report zero; strict types/Ruff/Bandit and legacy-token interoperability pass. Exact built-container OS scanning remains open without Docker/Trivy or authorized CI execution. |
| OPS05 readiness (partial) | `test_readiness.py`, `test_shared_store.py`, `test_slotctl_readiness_contract.py`; GET `/ready` and `/health` | `/ready` concurrently probes PostgreSQL plus the required shared store under one2-second budget; generic per-dependency503s expose no secrets, and separate single-flight locks prevent timed-out probe thread accumulation. Redis/SQLite/in-memory pings and candidate/stable/HAProxy `/ready` routing are covered. The 10/30-minute observer now checks internal and public `/ready` alongside liveness/login. Twelve focused and30 adjacent cases pass; independent review repeats all12. `/health` remains liveness. Release/application and active public verification still require infrastructure authorization. |
| SEC08 raw edits (fixed) | `test_security_hardening.py`, `test_department_soft_delete_migration.py`, `test_fresh_migration_bootstrap.py`; Data Console component/handler regression | Exact metadata/backend allowlist permits only named Department rename and deactivation; generic DELETE returns409 without mutation/audit. Revision0132 adds reversible `is_active` defaulting true, preserves historical references and rejects new inactive assignments. Deactivation is Super Admin only, audited atomically and idempotent. Independent gates pass84 focused backend cases,16 disposable-PostgreSQL migration cases, a separate authorization rerun, the frontend handler contract and strict TypeScript. Production migration remains separately gated. |
| PERF25 history candidates (fixed) | `test_sales_history_candidate_loading.py`, `test_customer_payment_history.py`; `/api/sales-orders/history` |SQL `UNION ALL` combines sales/production candidates, preserves sales-first ties and nulls-last ordering, and applies offset/limit before object/child hydration. Optional totals are counted in SQL; summaries are bounded to the selected page. Fifteen history/payment cases pass, including802 candidates with a10-row page. |
| PERF08 receiving queue (fixed) | `test_receiving_queue_query_growth.py` + workflow tests; GET/remove `/api/packages/receiving-queue` | 34 independent passes. Minimal rows default to50, cap at100 and omit child hydration; detail remains available by package ID. Queue memory/response size is bounded. |
| PERF19 cutting reconciliation (fixed) | `test_cutting_reconciliation_query_growth.py`, `test_cutting_reconciliation_postgres.py`; cutting reconciliation and bundle edits |Grouped reconciliation stays16/16/16 SELECTs at1/50/401 scopes with every evidence aggregate SQL-filtered to those scopes. Full bundle editing falls41/41/41→29/29/29 through one scoped UNION while preserving all quantity/identity branches. Twenty-eight focused cases and one real-PostgreSQL lock/rollback race pass. Remaining work is proportional to relevant evidence and required updates. |
| SEC03 audit chain | `test_audit_chain_concurrency.py`; every PostgreSQL `log_action` transaction |12 cases: simultaneous empty/existing-chain commits, nested commit/rollback, active savepoint commit, close/reuse, anonymous actor, callback/flush failure and actor-delete contention. Independently passes within30 PG audit/finance/receipt/retry cases. IDs/hashes appear only at outer commit; verify READ COMMITTED before rollout. Ordered actor locks precede global chain lock; one head read per transaction, O(audit rows) hashing/inserts. Historical damage and throughput capacity are not covered. |
| PERF12 package/bundle labels (fixed) | `test_package_label_query_growth.py`, `test_bundle_print_response.py`; package, print-run and bundle sheets |Package by-ID sheets remain12/12/14/14 SELECTs at1/50/401/500. Print-run sheets fall14/504/2004→14/14/14 and bundle sheets8/57/207→7/7/7 at1/50/200. Package sheets cap500; bundle/print-run sheets cap200, with over-limit rejection before rendering. Twenty-nine focused and12 compatibility cases preserve exact cards, canonical/orphan payloads, order, image projection and once-per-label QR work. |
| Revenue date crash | `test_finance_revenue_dates.py`; GET `/api/finance/revenue-by-period` | UTC, equivalent+05:00 and naive bounds select the same inclusive range without500. Missing issued_at uses created_at. Four API cases pass. Existing O(all invoices) loading and accounting policy remain unchanged; no FN08 completion claim. |
| PERF33 stocktake (fixed) | `test_stocktake_detail_pagination.py`, `test_stocktake_scan_evidence.py`, `test_warehouse_stocktake.py`, `test_stocktake_postgres_snapshot.py`; detail + CSV export |Detail serializes only the requested page and changed-detail uses9/10/14 SELECTs at1/50/401. CSV is complete and streamed in400-row keyset pages with1/1/2 page reads. PostgreSQL holds one read-only repeatable-read snapshot across all detail/export batches; two races prove a writer committed between batches cannot mix results, and generator close releases the transaction. Sixty-nine compatibility cases pass with3 expected local PG skips;3 disposable-PG cases pass. |
| FN07 sales inputs (partial) | `test_sales_item_numeric_bounds.py`, `test_sales_order_line_list_bounds.py`; POST `/api/sales-orders` | Nonfinite/oversized prices and int4-overflow quantity/pack counts return422 without order/item writes. The 1,000-line limit accepts its exact boundary; 1,001 rejects before writes and preserves unauthenticated 401 precedence. Explicit zero, omitted price and valid individual field limits remain accepted. Existing line validation is O(lines); aggregate totals and catalog-derived prices remain separate concerns. |
| PERF02 accessory queue (fixed) | `test_accessory_request_query_growth.py`, `test_accessory_request_postgres.py`; inventory accessory requests | PostgreSQL derives BOM demand, direct/indirect/manual-alias issues, stock/reservations, completion, exact totals and global status ordering before `LIMIT` in one statement. At1/50/401 candidates it returns1/10/10 rows with exact totals; the401-row plan runs in23.668ms and uses existing indexes. Seven PG parity/plan and14 independently rerun SQLite compatibility/auth cases pass. Legacy path is dialect-only fallback; PG errors propagate. |
| SEC09 password-change slice | `test_password_change_races.py`; change-password handler | Two stale sessions use the same old password: one200, one400, one audit, winning hash retained. Real PostgreSQL wait/commit test independently passes. Inactive/deleted stale users reject401 without writes. Three SQLite regressions included in55 passing compatibility cases. Final cross-workflow closure is covered by the SEC09 fixed gate below. |
| PERF35 bounded lists (partial) | `test_notification_api_contract.py`, `test_global_search_pagination.py`, `test_dashboard_active_production_pagination.py`, `test_super_data_query_growth.py`, `test_admin_user_pagination.py`, `test_role_pagination.py`, `test_purchasing_request_pagination.py`, `test_work_order_list_pagination.py`, `test_print_run_query_growth.py`, `test_package_change_request_pagination.py`, `test_storage_map_find_pagination.py`, `test_packaging_receipt_query_growth.py`, `test_payroll_record_pagination.py`, `test_paid_process_pagination.py`, `test_payroll_adjustment_pagination.py`, `test_payroll_period_pagination.py`, `test_finance_invoice_pagination.py`, `test_finance_revenue_pagination.py`, `test_cutting_passport_list_query_growth.py`, `test_sewing_assignment_pagination.py`, `test_sewing_record_history_pagination.py`, `test_sewing_flow_utilization_query_growth.py`, `test_quality_check_list_pagination.py`, `test_employee_pagination.py`, `test_hr_position_pagination.py`, `test_finished_goods_pagination.py`, `test_branded_stock_pagination.py`, `test_shipment_customer_pagination.py`, `test_shipment_eligible_order_pagination.py`, `test_shipment_ready_package_pagination.py`, `test_branded_order_parties_pagination.py`, `test_material_reservation_pagination.py`, `test_bundle_history_pagination.py`, `test_customer_payment_pagination.py`, `test_model_bom_item_pagination.py`, `test_collection_season_pagination.py`, `test_supplier_list_pagination.py`, `test_department_list_pagination.py`, `test_warehouse_list_pagination.py`, `test_purchase_order_pagination.py`, `test_forecast_recommendation_pagination.py`, `test_sewing_daily_report_read_query_growth.py`, `test_fabric_scan_summary_pagination.py`, `test_attendance_export_pagination.py`, `test_sewing_daily_export_pagination.py`, `test_stocktake_list_pagination.py`, `test_payroll_order_option_pagination.py`, `test_payroll_sewing_production_export_pagination.py` | Operational lists preserve legacy responses and add opt-in bounded SQL pages. At1/50/401 rows, returned-row work remains bounded and filters apply before count; order, totals, permissions and parity pass across search, dashboards, administration, purchasing, production, finance, payroll, cutting, sewing, HR, inventory, Finished Goods, shipments, catalog, suppliers, departments, forecasting, fabric scans and exports. Other lists/exports remain open. |
| PERF21 material origins | `test_traceability_material_usage_query_growth.py`; cutting material history | 1/50/401 distinct batches/items require1/1/2 SELECTs. Four independently passing cases preserve cached labels, missing batches, signed legacy amounts, fabric/beika fallbacks and grouping. Trips O(ceil(unique batches/400)); grouping/memory O(usage rows). |
| PERF06 (partial) | `test_payroll_bulk_query_growth.py`, `test_payroll_period_matching.py`; bulk payroll records | 1/50/401 records:11/60/415 SELECTs, audit2/51/402, other9/9/13. Bulk input is capped at500; 501 returns422 without payroll/audit writes. Automatic period matching uses a start-sorted sweep and highest-ID heap while preserving explicit IDs, inclusive boundaries and overlap precedence. Ten PostgreSQL period/return races pass. Reference trips are chunked; audit O(n) and legacy manual queries remain open. |
| PERF21 shipping | `test_traceability_shipping_query_growth.py`; traceability read aggregators | 1/50/401 shipment references:2/2/4 SELECTs; package warehouses1/1/2. Four independently passing cases preserve scalar payloads, order, direct/fallback customers including legacy0, absent references and no lazy fallback. Trips O(ceil(unique references/400)), output O(rows). |
| PERF04 | `test_bundle_accessory_gate_batching.py`; manual/batch sewing receipt | One accessory gate for1/50 transitioning bundles; incomplete gate rejects before bundle/log/assignment mutation. Scanner still checks individually. Another order or ended transaction cannot reuse context. Already-received/empty cases preserved. Ten independently passing focused cases +39 related. Gate component no longer O(bundles×accessories); per-bundle transitions/audits remain O(bundles). |
| SEC09 proxy slice | `test-reset-proxy-deadline.mjs`; forgot/reset password proxy handlers | Both upstream fetch and body stalls abort at15 seconds and return503; no automatic retries. The final20-case gate also covers exact body limits, input stalls and cleanup. Proxy ownership and backend body bounds are covered by the SEC09 fixed gate below. |
| WF07 | `test_sales_status_patch_guard.py`; PATCH `/api/sales-orders/{id}` | Changed/null/bogus status returns409 without notes/status/audit changes, including admin. Same-status/omitted-status edits work; dedicated confirmation still advances draft. Eleven focused +38 sales cases pass. No extra SQL; constant scalar check. External manual-status callers must use workflow commands. |
| PERF21 cutting | `test_traceability_cutting_query_growth.py`; traceability cutting histories | 1/50/401 rows:16/16/19 full SELECTs; each batch/supplier/warehouse component1/1/2. Four focused tests independently pass; follow-up set-based dedupe retested by author. Preserve scalar payload, first-seen material ordering, missing-reference gaps and strict batch scope. Chunked lookup trips; expected O(rows) dedupe, no total O(1) claim. |
| FN04 alternate route | `test_invoice_creation_integrity.py`; finance invoice and sales generate-invoice | Concurrent finance/finance, sales/sales and mixed callers return one invoice with one audit. Six real PostgreSQL races pass. HTTP replay flags and refreshed cached amount/status tested. Both acquire order before numbering; bounded locking/lookups, not a throughput claim. |
| PERF03 | `test_reservation_plan_query_growth.py`; production reservation plan | 1/50/401 requirements:14/14/21 SELECTs. Five independently passing cases cover scalar parity, signed movements, unit/FIFO eligibility, partial/consumed/cancelled reservations and401 exact batches sharing an item. Chunked trips O(ceil(items/400)+ceil(batches/400)); exact suggestions O(requirements). Unbound suggestions still scan matching candidates; SQL aggregation/output scale with data. |
| FN06 | `test_idempotency_owner_races.py`; keyed mutation retries | Same caller/key/payload returns one result and one payment; different caller or payload gets409; rollback releases key. Unknown legacy owner gets409 before response disclosure. Four real PostgreSQL cases pass. Bounded key lookup/lock; waiting depends on the original transaction. No-key requests remain outside retry protection. |
| PY05 | `test_payroll_qr_return_races.py`; QR return | Concurrent returns produce one success; return/finalizer block in either order; changed period assignment rejects409 without mutation. Four real PostgreSQL cases independently pass. Six SQLite compatibility cases pass. Bounded locks, no throughput claim. |
| FN07 (partial) | `test_finance_input_bounds.py`, `test_payment_integrity.py`, `test_customer_payment_cents.py`, `test_customer_order_history_pagination.py`, `test_1c_payment_reassignment.py`, `test_idempotency.py`; invoice/payment inputs and finance aggregates | Negative/nonfinite/overflow and sub-cent payment amounts reject before writes. Manual/customer payment allocation, customer history and 1C paid-state recomputation use Decimal; exact $0.01 settlement, $0.01 outstanding, replay and no-write boundaries pass. Combined exact-cent selection passes 97 with14 expected PostgreSQL-only skips. Order profit, branded stock value and cost breakdown use Decimal intermediates while preserving float response fields. Manual vs 1C policy for a $0.01 payment remains unresolved; purchase/stock-batch unit-cost request precision exceeds Numeric(12,4), with reject-vs-round policy unresolved. Other derived/accounting rules and state/list families remain open. |
| FN07 purchasing inputs (partial) | `test_purchase_request_quantity_bounds.py`, `test_purchase_cost_bounds.py`, `test_purchase_order_quantity_bounds.py`, `test_purchase_receipt_quantity_bounds.py`, `test_price_calculation_purchasing_bounds.py`, `test_price_calculation_finance_bounds.py`, `test_customer_payment_amount_bounds.py`, `test_accessory_issue_quantity_bounds.py` | Purchasing request, cost, order and receipt numbers are constrained to finite PostgreSQL-representable values before writes; the accessory-issue line input is capped at 1,000. Existing reference/auth/business precedence is retained. Price-calculation purchasing/finance inputs and customer payment cents have focused boundary regressions. Derived financial policy and remaining input/state families stay open. |
| PERF21 (fixed) | `test_traceability_bundle_query_growth.py`, `test_traceability_batch_work_order_query_growth.py`, `test_traceability_history_query_growth.py`, `test_traceability_postgres_plan.py`; traceability | Bundle reads remain9/9/10 with department queries1/1/2. Production-batch graphs remain17/17/17 SELECTs with one WorkOrder statement/target hydration; populated package histories remain21/21/21 and hydrate one matching row per family. Empty exact matches retain legacy full-PO fallback. The complete focused selection passes63 with one PG-only skip; the real PostgreSQL17 indexed UNION plan passes. Response construction remains O(returned graph rows). |
| PERF17 | `test_sewing_daily_report_read_query_growth.py`; daily reports and line context | Cold1/50/401 orders:report5/5/9 SELECTs, context10/10/14. Scalar model/image/passport parity, date/id tie ordering, manual override, factory/flow/date filters and summary preserved.400-ID chunks: lookup trips O(ceil(n/400)); processing/assets/SQL ranking still scale with rows. Writes and locks unchanged. |
| WF11 | `test_usluga_handover_integrity.py`, Usluga suite; full/material PATCH and handover | Two simultaneous handovers: one200, one409, one scan/audit. Material edit may finish before package-blocked handover, never after it; package membership change while waiting gives409 without mutation. Three real PostgreSQL forced-wait races independently pass. Locks/checks process O(packages); no speed claim. |
| PY02 | `test_payroll_period_races.py`, payroll/control/reversal/payable suites | Writer and finalizer wait on same period; reversed bulk order does not deadlock. Approval blocks void; automatic scan waiting behind finalization returns409 without orphan record. Cached status/Control token refresh; same-key replay survives finalization. Six PostgreSQL races pass and are independently repeated. Bulk still O(n) reads/writes with O(n log n) lock-key sorting; not a speed fix. QR returns separatePY05. |
| PERF22 (fixed) | `test_usluga_list_query_growth.py`; GET `/api/usluga/orders` | Default and explicit pages cap at100; row children are page-scoped. The sole UI consumer uses paged SWR/load-more. Twenty-seven backend cases and the Bun frontend contract pass independently. |
| PERF24 | `test_sales_list_query_growth.py`, `test_shipment_list_query_growth.py`; sales and shipment lists | Sales1/50/401:1/1/1 SELECTs, two with total. Shipments1/50/501:3/3/4 from selectin package chunks. Filters, shared/null/override customers, paging/order, package quantities and distinct valid scan totals preserved; HTTP response and401 tested. Output O(parents+children); database filtering/count/scan-log work is data-dependent. History/write performance separate. |
| PERF23 (partial) | `test_price_calculation_list_query_growth.py`, pricing workflow suite; GET `/api/price-calculation/requests` | Cold1/50/501 distinct models:4/4/7 SELECTs; no image BLOB reads. Full response/order, sizes, image fallbacks and calculated values preserved. ORM batches500 model IDs; query growth O(ceil(n/500)), output/asset processing grows with data. Unbounded list and polling remain open. |
| WF12 | `test_sewing_daily_report_lock_order.py`; daily report create/update/delete | Disposable PostgreSQL: writer visibly waits for work-order lock; holder can still lock parent NOWAIT, then writer finishes. Parent-wide capacity across different work orders remains enforced. Bounded lock reads, no throughput claim; assignment accounting is separate. |
| PERF16 | `test_sewing_flow_utilization_query_growth.py`, `test_single_flow_utilization_query_growth.py`, flow scope suite | Snapshot1/50/401 added flows incl seeded lines:3/3/5 SELECTs; single-flow1/50/401 work orders:3/3/3. Statuses, split/direct, expired/missing/future dates, over-completion, rounding and factory scope match.400-ID snapshot chunks; Python O(rows + flow sorting), SQL plan/index cost remains data-dependent. |
| PERF36 (fixed) | `test_task_broadcast_batching.py`, `test_notification_fanout_batching.py`, `test_super_data_query_growth.py`, `test-super-data-mutation-policy.mjs`; fan-out + Data Console |Task and notification discovery use a shared cap+1 ceiling before writes. Task broadcasts load only authorization fields:1/50 recipients use4/4 SQLite or6/6 PostgreSQL SELECTs and two flushes; PostgreSQL uses two batched INSERT statements. At401 recipients with cap250, one bounded query returns251 rows and rejection occurs before policy iteration or task/notification/audit writes. Data Console1/50/401 table directories issue0 count SQL. Combined local selection80 passes/10 PG skips, PostgreSQL selection17 passes and independent PG broadcast rerun8 passes; necessary fan-out rows remain O(N). |
| PY03 | `test_payroll_payable_authorization.py`, `check-payroll-payable-authorization.mjs`; process QR and payroll APIs | Scan-only issuance/freeform pay reject403; tampered scan values use trusted label; mixed bulk rolls back; retry replays once. Manager/manual workflows work. Actual React renders preserve scan-only printing; identical valid handler fixture POSTs only for manager. Review custom scan-only issuer roles before rollout. No extra per-record lookup; bulk stays O(n). Visual browser QA remains. |
| PERF29 (partial) | `test_customer_payment_query_growth.py`, `test_payment_integrity.py`, `test_1c_payment_reassignment.py`; payment/1C allocation |1/50/401 customer candidates use2/2/3 SELECTs. Grouped 1C status refresh reuses the affected invoice load for its race synchronization hook, with at most two invoice reads. Reassignment, rollback, balances/status and advance split remain covered. Other 1C row resolution remains. |
| DB02 (partial) | `test_hr_workspace_validation.py`, `test_purchase_request_status_validation.py`, `test_branded_order_type_contract.py`; employee, purchase-request and branded planning-order commands | Employee status accepts the four established UI codes. Purchase-request creation accepts only `draft`/`pending_approval`, including legacy whitespace/null normalization. Branded planning order creation advertises `milana`/`eco_cotton`/`besttex`/`customer` as stable enum codes and preserves trim/case normalization; five unsupported-value cases return422 without order/audit writes, unauthenticated calls retain401, accepted `BESTTEX`/`Eco_Cotton` normalize to canonical values, missing customer retains404 precedence, and OpenAPI lists the codes. The 10-case command contract passes within a 26-case focused/adjacent planning selection. Generic Production Order status policy and other loose commands remain open. |
| PERF30 family operations (fixed) | `test_catalog_family_operations_query_growth.py`, `test_catalog_family_operations_postgres.py`, `test_catalog.py`, `test_catalog_rename_integrity.py`, `test_model_family_approval.py`, `test_usluga.py` |Clone allocation preserves global lowest-gap numbering with exact400-code batches:1/1/2 code-only reads for1/50/401 occupied codes. Rename uses2/2/2 filtered reads; approval uses1/1/1. Sixty-six compatibility and6 disposable-PostgreSQL cases pass. PostgreSQL plans use the code and generated group-key indexes without sequential scans; concurrent colliding namespaces serialize and rollback permits number reuse. |
| WF02 | `test_work_order_command_scope.py`; generic work-order PATCH/start/complete/block/unblock | Wrong stage/factory returns403 without mutation/audit. Selected-factory grants, own stage, planning/admin and valid ECO Usluga work; misassigned legacy Usluga cannot bypass ECO. The owner retained authorized generic completion without stage evidence, and its regression verifies completed state plus one audit event. All27 focused cases pass. |
| PERF37 | `test_rate_limit_scheduling.py`; global middleware | Eight simultaneous calls against a real temporary SQLite counter allow3/reject5 with valid Retry-After; increment/TTL execute off-loop. Concurrent initialization creates one store. Health/OPTIONS/disabled bypass storage. No asymptotic query reduction: synchronous work is moved to bounded workers; SQLite lock contention/capacity still need load tests. |
| PERF38 (fixed) | `test_rate_limit_scheduling.py`, selected `test_security_hardening.py`; global middleware/shared store | Two signed users behind one IP receive independent sequential and simultaneous budgets; at limit3, each user's eight concurrent requests produce exactly3 accepted/5 rejected. Anonymous, invalid/expired credentials and every public login/reset path remain IP-scoped. Unknown signed users still receive route-level401, trusted proxy rules remain, SQLite fixed-window cleanup passes, and Redis increment/initial TTL is one atomic operation. Exact final diff:25 focused passes; preceding combined auth/security selection:29 passes. |
| PERF14 (fixed) | `test_cutting_passport_defaults_query_growth.py`, `test_cutting_passport_list_query_growth.py`, `test_material_reservation_query_growth.py`, `test_material_reservation_concurrency.py` | Cold1/50/401: defaults10/10/11 SELECTs; lists4/4/7 with no image BLOB reads. Reservation helper11/11/11 and full Cutting addition15/15/15 SELECTs; item locks, numbering locks, number reads and reservation/material-plan/audit inserts each transfer1/1/1. Preserve input/error/audit order, rollback number reuse, factory/auth and concurrent lock order. Fourteen local cases pass/17 PG-only skip;28 disposable-PG cases pass. Necessary row writes/output remain O(N). |
| WF01 | `test_production_order_update_integrity.py`; production-order PATCH | Internal/source/identity/relationship fields reject422; invalid targets reject404 without writes. Supported edits/coercion/null clearing work; Usluga stays409 and denied caller403. Typed OpenAPI remains. Added target reads are bounded; stage transitions/concurrent edits are separate findings. |
| PERF40 (partial) | `test_image_upload_scheduling.py`, `test_image_storage.py`, `test_settings_patch_integrity.py`, `test_hr_document_upload_lifecycle.py`, `test_model_image_upload_lifecycle.py`, `test_production_attachment_upload_lifecycle.py`, `test_sales_attachment_upload_lifecycle.py`, `test_attendance_photo_upload_lifecycle.py` | Upload conversion/disk/thumbnail hooks run off-loop, one conversion at a time. Thumbnail failure restores earlier/pre-existing targets. Failed/cancelled company-logo, HR-document, model-image, production-attachment, sales-attachment and attendance-photo paths clean only new files and preserve prior targets. The existing 25 focused cases/4 PostgreSQL skips cover the core image-storage/settings behavior; newer route lifecycle cases are separately committed. Processing still scales with pixels/bytes; other routes, SQL and cross-process capacity remain. |
| UI03 (fixed) | `test_manual_receipt_reconciliation.py`, `test_purchase_receipt_idempotency.py`, `test_shipment_creation_lock.py`; three frontend recovery scripts | Manual receipt, print-run/package creation, manual shipment and purchase receipt recovery never repost the mutation. Owner/fingerprint isolation, active authorization, deleted/revoked result hiding, cancellation tombstones, localStorage/Web Locks/storage events, legacy migration and newer-key preservation pass. Twenty-six local backend cases/7 PG skips, seven real-PG races, full frontend stabilization, strict types, targeted lint and Ruff pass. |
| PERF15 | `test_planning_query_growth.py`; planning requirements/estimate | Cold requirements 1/50 lines use 6/6 SELECTs; estimates 1/50/401 use 9/9/15. Preserve shared/size/color BOM, signed stock, batchless movements, active reservation subtraction, nullable items and ordering. Chunked lookup queries; Python work includes each sales-line/BOM match plus O(n log n) key sorting. Not O(1) total work. |
| SEC09 token/profile slice | `test_auth_credential_cutoff.py`; auth/session profile | Issue token at .100s, rotate at .500s: bearer/cookie reject401; .750s token succeeds. Missing/malformed issue time rejects after rotation. BST profile PATCH preserves factory fields. Final race/proxy coverage is recorded in the SEC09 fixed gate below. |
| WF03 | `test_package_evidence_integrity.py`; single/bulk package APIs | Missing/zero output returns409, 7 available cannot create two5-piece packages, and failure leaves no package/item/stock. Output10 permits two5-piece packages. Uses existing aggregate query; no speed claim. |
| UI04 | `frontend/scripts/test-dashboard-live-data.mjs`; management dashboard | With management permission, request live overview; finance requires its own permission. Synthetic live values appear; loading/error never falls back to demo figures.15 actual React render cases; manual browser check remains. |
| UI05 | `frontend/scripts/test-home-stage-kpi.mjs`; Home | Four stages of100 display400 as stage activity, with an explicit unique-output warning in EN/RU/UZ. Actual React render; browser layout still needs verification. No query/arithmetic change. |
| AT06 (fixed) | `test_attendance_source_versions.py`, `test_attendance_import_races.py`, connector tests; roster/events imports | Bundled connector captures a timezone-aware source version before device reads; durable per-device checkpoints advance under the existing lock without a migration. Late older rosters and metadata are ignored observably, while stale/unversioned event rows remain append-only. Legacy unversioned clients retain pre-adoption behavior; rollback preserves retry. Focused attendance32, connector44 and disposable-PostgreSQL8 pass; independent reruns confirm6/44/8. |
| PERF28 (fixed) | `test_purchasing_query_growth.py`, `test_purchase_order_reference_batching.py`, `test_purchase_request_approval_reference_batching.py`, `test_purchase_receipt_idempotency.py`; request/order/approval/receipt services |At1/50/401 lines, approval supplier reads are1/1/2, request references2/2/4, order references3/3/6 and receipt references4/4/8; audit-head reads are exactly1 for every operation.140 focused cases pass with5 PostgreSQL-only skips. Required per-line persistence and chain hashing remain O(N), not repeated reference/head reads. |
| WF04 | `test_packaging_quantity_validation.py`, `test_printing_quantity_validation.py`, `test_cutting_quantity_validation.py`, `test_sewing_record_quantity_validation.py`; production records | Packaging, Printing and Cutting reject unsafe numeric bounds and impossible conservation. Sewing rejects unsafe numeric bounds, sewn above input, passed above sewn and processed totals above explicit input. Failed/rejected input may sit beside good sewn output, and input_qty=0 inference remains supported. Invalid Sewing POSTs leave sewing/replacement/waste/notification/audit rows unchanged. Constant-time schema validation. |
| WF05 | `test_package_model_integrity.py`; single/bulk package APIs | Wrong header/item model rejects with 400 and creates no stock graph, even with admin override. Matching package still succeeds. Adds no queries; existing item validation remains O(n). |
| WF06 | `test_packaging_stale_counters.py`; packaging receive/record APIs | Cached counters refresh before increment. Disposable PostgreSQL forces both receipt writers to wait: 5+3+4 ends at12 with two receipts. Adds one target lock/read; no full-path complexity claim. |
| AT01 | `test_attendance_event_results.py`; attendance overview, XLSX and HR | Failed/unknown scans remain raw but do not create attendance/hours. Success and legacy NULL results count. Check used/not-used filters and exports. Adds a SQL predicate, not per-row queries. |
| PERF42 | `test_sewing_line_context_query_growth.py`; line context | 1/12 assignments:2/2 capacity queries, previously5/60. Same order/batch/assignment limits, top/bottom values and legacy fallback. Python aggregation is O(grouped history + displayed rows), not O(1) total work. |
| AT04 | `test_attendance_historical_profiles.py`; Attendance overview/daily XLSX | Import a profile and two scans, remove profile through a full snapshot, then reopen that date: hours still appear once in overview/export. Other dates and factories remain excluded. Query round trips remain bounded; database aggregation still grows with matching history. |
| AT05 | `test_hr_attendance_validation.py`; HR attendance/settings | UTC events on either side of Tashkent midnight appear on the correct day. Invalid legacy hours fall back safely; nonfinite input returns 422. Factory settings read once. |
| PERF34 | `test_shipment_document_query_growth.py`; shipment documents | 1/20 manual packages use 4 SELECTs. Exact/wildcard/ambiguous prices, line order, basis hash and frozen document remain unchanged. Inputs indexed once: O(P+I+L+output) processing, not constant total work. |
| ST09 | `test_finished_goods_damage_integrity.py`, `test_finished_goods_reserve_integrity.py` | Damage→reserve and reserve→damage both reject the unsafe side. Package/stock lock order is shared. Disposable PostgreSQL reserve/damage suite6 passed. |
| PERF20 cutting/packaging reads (fixed) | `test_usluga_cutting_batch_query_growth.py`, `test_packaging_receipt_query_growth.py`, `test_cutting_packaging_write_query_growth.py`, `test_cutting_packaging_write_postgres.py` |At1/50/401, full cutting bundles use41/41/41 SELECTs, mixed reserved/direct material consumption30/30/30, packaging BOM/stock27/27/27 and Usluga rejection18/18/18. Strict duplicate-batch lines accumulate reservation demand in input order. Focused32, adjacent inventory36 and broad compatibility120 pass; all12 disposable-PG query-growth/plan/concurrency/rollback cases pass. Necessary row writes/output remain O(N). |
| ST10 | `test_purchase_conversion_integrity.py`, `test_purchasing.py`; request conversion API | Two simultaneous conversions create one order; losing request returns 409. Conversion racing rejection preserves one valid final state. Rollback allows later conversion; cached old approval does not bypass state checks. |
| ST07 | `test_finished_goods_reserve_integrity.py`; finished-goods reserve API | Two reserves of 6 against 10: one succeeds, one rejects; stock stays 4 available/6 reserved. Reserve racing release preserves the other order; reserve racing dispatch never restores sold stock. |
| API04 | `test_hr_workspace_validation.py`; HR organization/positions/recruitment/calendar/uploads | Wrong-factory links reject; salary/date inversions, nonfinite/oversized input and blank titles reject without writes. Valid scoped workflow and explicit optional clearing still work. |
| PERF31 | `test_attendance_people_query_growth.py`, `test_attendance.py`; integration people API | Create/update 1/10/50 people with 3 SELECTs; 401 with 4. Duplicate snapshot rolls back; partial snapshot keeps absent people; full snapshot affects only its device. |
| WF08 (partial) | `test_waste_sale_integrity.py`; waste sale API | Sell4 then6 of10: stays received then sold; overage rejects. Every new sale requires Idempotency-Key, and keyed uncertain requests reconcile/tombstone under the same lock order without a second physical sale; changed payloads conflict. Broad waste tests pass115 with9 expected PostgreSQL skips and all5 real-PostgreSQL races pass; required-key review passes39 disposable-PostgreSQL cases and the frontend recovery contract. Historical duplicate repair and valuation policy require authoritative data and owner decisions. |
| ST08 | `test_finished_goods_release_integrity.py`; finished-goods release API | Shipped/consumed/nonpositive reservations return 409 without changing stock. Intact sibling reservations release correctly. Two simultaneous releases restore once; release racing dispatch never restores sold goods. Run the two `postgres` cases on disposable PostgreSQL. |
| PERF11 | `test_print_run_query_growth.py`, `test_package_workflows.py`; GET `/api/packages/print-runs` | 1/10/50 runs use 3 SELECTs. Manifest corruption rejects; deleted members stay hidden; factory filtering, original payload and 100-run cap remain intact. |
| SEC06 | `test_admin_membership_delete_race.py`, `test_user_audit_history.py`; isolated PostgreSQL | Concurrent deletes of the last two wildcard admins: one succeeds, one rejects, one admin remains. Concurrent audit insertion must still complete and preserve history. |
| WF10 | `test_waste_readonly.py`; GET `/api/waste` | Change a synthetic batch cost, then read waste. Live response estimate changes; persisted historical value and finance totals do not. GET never commits. |
| ST01 | `test_purchase_receipt_idempotency.py`; Purchasing → Receiving | Receive 5; lose the response; reload and retry the saved request. Still 5, one receipt. Changed payload with the old key rejects. Concurrent same-key submissions replay one result. |
| ST02 | `test_inventory_movement_integrity.py`; inventory movement API | Issue 4 from 10: batch and movement agree at 6. Wrong item/batch, unit, location or invalid amount rejects without writes. |
| ST03 | `test_inventory_movement_integrity.py`; warehouse balances | A W1 movement does not change W2. Transfer changes the two warehouse balances but not the global total. |
| ST11 | `test_material_reservation_concurrency.py`; reservation/Cutting APIs | Concurrent claims cannot exceed stock. Retry/release and sibling-batch contention remain consistent; no lock-order timeout. |
| FN02 | `test_payment_integrity.py`; payment and advance allocation APIs | Concurrent payments leave invoice totals/status matching committed payments. Existing overpayment/advance rules remain valid. |
| FN04 | `test_invoice_creation_integrity.py`; `POST /api/finance/invoices` | Concurrent creation for one order returns one invoice, not duplicate billing. Other entry points are not covered by this fix. |
| SEC04 | `test_static_file_auth.py`; `/model-files/...` | Disabled/revoked account cannot download with its old cookie. Active authorized account still can. |
| SEC05 | `test_reset_token_revocation.py`; password reset | After one reset succeeds, sibling links and pre-reset sessions fail. Concurrent reset requests cannot both win. |
| SEC07 | `test_assignment_delete_scope.py`; assignment DELETE | Wrong factory rejects; right factory deletes only the permitted assignment. This does not cover every factory-scoped endpoint. |
| SEC07 | `test_flow_utilization_scope.py`; `GET /api/sewing-flows/{fid}/utilization` | Wrong selected factory returns 403, including Super Admin sessions. Select the explicitly permitted factory to read its line; missing line remains 404. |
| SEC10 | `test_privileged_user_delete.py`; Users → Delete | Limited administrators cannot delete a privileged administrator, including policy-based privilege variants. |
| UI01 | `test-session-recovery.mjs`; authenticated page | Temporary 503/network failure shows recovery without discarding the session. Bounded retry succeeds; genuine 401 redirects to login. |
| UI02 | `test-api-deadline.mjs`; API client | Hanging JSON/form/error response bodies time out; cancellation works; ordinary responses remain unchanged. |
| PY01 | `test-payroll-scan-order.mjs`; Payroll → Scan | Queue badge A, work A, manual A, badge B, work B. Recorded ownership follows the event sequence, not the latest UI selection. |
| PERF01 | `test_package_query_growth.py`; package list | Same rows/filters/order; no per-package query growth. See complexity table below. |
| AT02 | `connectors/hikvision_attendance/test_event_checkpoint.py` | Mock device advertises 3 events but returns 2 then an empty page. No upload/checkpoint advance. Complete retry imports all 3. HTTP/upload retries keep event identities. |
| SEC02 | `test_user_audit_history.py`; Users → Delete/Edit | Delete an account with authored history: 409 and no cleanup. Edit → deactivate works; old session rejects. A concurrent audit insert also preserves actor/hash and rolls back cleanup. |
| API06 | `test_name_authorization.py`, `test-name-authorization.mjs`; profile/pricing | Rename restricted user to `Abbosbek` or matching email. Same session still gets 403 from pricing GET/PATCH; navigation remains hidden. Explicitly granted users retain access. |
| PERF41 | `test_eco_history_query_growth.py`, `test_eco_transfers.py`; Eco transfer history | Pagination/date/order/roll rows/global totals unchanged. Empty page works; historical snapshot is not rewritten. |
| SEC01 | `test_scoped_permission_grants.py`; Users/roles APIs | Limited admins cannot grant wildcard/admin.super or change foreign-factory grants. Legitimate held permissions work; unchanged grants survive profile edits. |
| SEC07 | `test_passport_get_scope.py`; passport GET | Own-factory linked passport returns unchanged data; wrong factory 403; missing record 404. Manual unlinked passports retain the existing read policy. |
| DB04 | `test_fresh_migration_bootstrap.py`; isolated PostgreSQL | Empty database upgrades to 0131; rerun mutates nothing; 0130 row survives upgrade. Exact reviewed schema-drift baseline must match, not silently regenerate. |
| ST05 | `test_accessory_return_balance.py`; accessory issue/return APIs | Stock issue 8 + manual issue 4 allows returns 3+6+3. Retry changes nothing; excess rejects. Warehouse = 20−8+returns; production net = 12−returns. |

For UI QA, start a separate loopback-only frontend/backend with fresh synthetic accounts and database. Block external browser requests and backend connections. Test both a restricted and explicitly permitted account. Inspect the rendered error/success state and browser errors; screenshots must contain synthetic data only. Attendance connector and backend-only concurrency cases use protocol/transaction tests instead of screenshots.

## Measured complexity

Additional acceptance checks:

| Bug | Test / where | Expected result |
| --- | --- | --- |
| AT03 | Connector `test_sync_stage_isolation.py` | Roster fails but events still upload; job reports roster failure. Failed event windows do not advance; TLS failure stops all work. |
| ST06 | `test_accessory_return_concurrency.py`; return API | Two returns of 7 against 10 cannot both commit. Same-key replay creates one return; rollback frees allowance; unrelated orders do not block each other. |
| WF09 | `test_waste_disposal_integrity.py`; Waste disposal | Received → pending → approved → disposed. Repeat/stale decisions reject without writes. Invalid historical pending records can still be rejected safely. |
| PERF07 | `test_payroll_label_query_growth.py`; payroll QR list | Global counts, page filters, canonical aliases and original payloads remain correct. Ambiguous references still reject. |
| API01 | `test_task_assignment_authorization.py`; task PATCH | Creator without manager permission cannot change another assignee or unassign. Ordinary edits and assignee status-only updates still work. |
| API03 | `test_settings_patch_integrity.py`; Settings | Change phone only: address/logo survive. Invalid fields return 422 without writes. Parallel patches and logo uploads preserve both changes; missing-row creation produces one row. |

`n` = returned parents; `r` = returned child rows; `p` = permissions; `a` = audit rows. Query counts include authentication in these test fixtures.

SEC06 locks/scans active users: O(u) membership work, intentionally serialized; not O(1). WF10 removes writes, not read-query growth: per-row cost lookups and unbounded listing remain. Live waste estimates versus stored accounting snapshots (including automatically created zero values) still need an agreed FN08 valuation policy.

PERF11: 1/10/50 runs fell from 3/12/52 to 3/3/3 SELECTs (100 returned runs: 102 → 3). Bounded database round trips for the existing 100-run page; O(n+r) Python work/memory for runs and members. Individual member counts and total database work are not constant.

ST08 uses a bounded number of reads but locks/scans all r reservations for the selected stock: O(r) Python work/memory. Inconsistent historical balances reject rather than auto-repair. ST07 locks the same package/stock before reserving, adding a bounded number of reads; it does not establish constant total SQL/audit cost.

PERF31 lookup round trips are O(ceil(n/400)); payload, processing and writes remain O(n). WF08 scans s prior sales for one record: O(s) work/memory, a fixed number of lookup queries, not constant total database work. API04 adds at most a few reference lookups per command; no per-list optimization claim.

ST10 replaces an unlocked parent read with one locked/refreshed read. Conversion still processes n request lines and retains per-line reads/writes: O(n); no N+1 optimization claim.

| Path | Before → after | What remains |
| --- | --- | --- |
| Package list, 50 simple packages | 152 → 5 SELECTs; 1/10/50 = 5/5/5 | O(n) output work. Distinct-model fixture is 9/9/9; order fallback 7/7/7. SQL filtering/counting costs depend on data/plans. |
| Eco history, 50 dispatches | 54 → 5 SELECTs; 1/10/50 = 5/5/5 | O(n+r) grouping/output; O(n+r) memory. Global totals, count, sort and offset still cost database work. Empty page: 4 queries. |
| Attendance download | O(pages) device requests | O(events) processing/memory. Failure must not become a successful checkpoint. |
| Pricing authorization | No added DB queries | O(p) permission evaluation; profile text no longer grants access. |
| Audited account deletion | One additional history-existence lookup | Worst-case O(a) without an actor index. This is an integrity fix, not a speed claim. |
| Payroll label list | 1/10/50 global groups: 9/36/156 → 9/9/9 SELECTs | Alias page fixtures use 14; 401 references use 12. Queries grow by batches of 400, not per label. Global aggregation/output remain proportional to history/groups; not O(1) total work. |
| Return/disposal/settings guards | Bounded added lock/reference queries | Waiting depends on contention; existing audit/database work is not benchmarked. |
| Scoped grants / passport access | No new grant queries; bounded linked-order lookups | Permission-set work grows with permissions. Passport serialization still depends on materials; no speed claim. |
| Accessory return summary | No added queries | O(stock issues + manual issues) grouping; existing O(groups log groups) sorting. This fixes totals, not pagination or concurrent returns. |

**Bounded round trips are not O(1) total work.** Unmeasured APIs remain unknown. Local timings under laptop memory pressure are not a 200–2,000-user capacity claim.

## API coverage ledger

The saved 510-route report is historical. CI `df42f1b` observes511 routes:420 hit,409 successful,258 rejected,91 unobserved; categories overlap. Evidence: run35544548187 artifact `backend-test-evidence` (6,148 requests). Refresh again on the final revision.

From `backend/`, run a selected test file with HTTP observation:

```text
python scripts/report_api_test_coverage.py run --output ../outputs/api-batch.json -- app/tests/test_flow_utilization_scope.py -q
python scripts/report_api_test_coverage.py merge --output ../outputs/api-merged.json ../outputs/api-batch.json ../outputs/another-batch.json
```

CI runs all `app/tests/test_*.py` files through this observer and uploads `backend-test-evidence`: route/status JSON plus JUnit failures. PostgreSQL concurrency remains a separate job. At `5a9f3fb`, inventory has **511 HTTP method/route pairs**, excluding docs/static mounts/WebSockets. Statuses separate success, rejection, errors and unobserved routes. A 2xx is not proof of correct stock or permissions; inspect assertions too. Direct service calls are not HTTP coverage.

The tool refuses an application `.env`, uses the existing temporary SQLite fixture, clears external-service settings and blocks non-loopback Python sockets. It is not a sandbox for arbitrary tests/native subprocesses. Reports contain route patterns, aggregate outcomes, Git revision and dirty-state flag—not request data or concrete record IDs. Only combine compatible inventories; a merged old run is not proof of the final implementation. Do not interpret instrumented timing as a benchmark.

## Release gates still required

### Latest reviewed fixes — local only

- **Current ledger:** 105 fixed, 16 partial, 6 open; 22 of 127 remain. Committed history through `f25b064` is pushed to `feat/ismoiljon`; later closure commits remain local pending separate approval.
- **PERF35 Package Scan partial continuation:** `4487ed0` replaces the full map graph on Package Scan with one fixed-cell grouped summary and exact-total model-scoped pages (50 default, 100 maximum), then displays Load more. The new 1/50/401, summary-parity, auth/no-write and SQL-bound tests pass5; the combined storage-map selection passes13. Strict TypeScript, Ruff and the executable Bun page harness pass. Main warehouse-map/stock, inbox, HR and payroll work remains; no ledger count changes.
- **PERF35 HR Organization partial continuation:** `ef94d3c` adds 50-row default/100-row maximum SQL pages, escaped factory-scoped search, exact unit/employee totals and SQL KPI counts. Manager/parent labels do not depend on which directory page is loaded, and the actual screen supports Load more for both hierarchy and action choices. Combined HR tests pass46; Bun screen test, strict TypeScript, Ruff and diff checks pass. HR attendance and other graph/queue scopes remain; count unchanged.
- **PERF35 payroll, inbox and HR attendance partial continuation:** `d7e4f15` gives Payroll Order QR options exact totals beyond 500 and 50-row screen paging (5 local, 2 disposable-PG cases); `59fea8b` bounds/searches Finished Goods inbox packages with exact package/order-group totals and preserves reservation-backed ready-to-ship rows (12 local, 5 disposable-PG cases); `a877454` pages HR attendance employees while retaining exact whole-factory/day cards via streamed grouped scan inputs (18 local, 4 disposable-PG cases). Frontend Bun wiring/behavior, strict TypeScript, targeted lint and Ruff pass for these slices. Broader inbox/warehouse/payroll graphs remain; count unchanged.
- **PERF35 warehouse-map partial continuation:** `67d3ca8` replaces the full placement graph on the main map with exact cell/zone aggregates, capped rack previews and selected-cell 50-row pages. The map's mixed-model move check now runs server-side under cell-scoped transaction locks; legacy clients remain unguarded as before. Thirteen focused and four adjacent local tests, one isolated-PG concurrent-move rollback test, 19 Bun screen checks, the full frontend stabilization suite, strict TypeScript, targeted ESLint, Ruff and diff checks pass. Warehouse stock, broader inbox/payroll graphs and other unbounded scopes remain; count unchanged.
- **PERF35 Finished Goods reservation partial continuation:** `deef9c6` adds bounded 50-order ready-to-ship pages with exact total and per-order reservation/package/item details. SQL windowing returns only the latest shipment for selected orders, and the no-limit API path keeps its legacy response. Eleven focused/adjacent local backend cases, the executable frontend paging harness, full frontend stabilization, strict TypeScript, targeted ESLint, Ruff and diff checks pass. Other inbox queues and large graphs remain; count unchanged.
- **PERF35 warehouse-stock partial continuation:** `b38f723` groups SQL detail rows before 50-row pagination and computes exact filtered totals and top model cards independently of the visible page. Legacy unplaced receipt aggregation/search and package-label previews remain intact. Five focused cases pass on SQLite and disposable PostgreSQL; the combined root stock/map selection passes14, as do strict TypeScript, targeted ESLint, Ruff, full frontend stabilization and diff checks. Other large workflows remain; count unchanged.
- **PERF35 payroll-summary partial continuation:** `2174ade` pages 50 employee/currency groups with exact global totals, debounced employee search and operation aggregation restricted to visible groups. The UI labels operation totals as partial until more groups load. Fifty-eight adjacent backend cases, strict TypeScript, targeted ESLint, Ruff, full frontend stabilization and diff checks pass. Headline totals still scan all matching records; other workflows remain and the count is unchanged.
- **DB01 partial follow-up:** `df14ad6` protects sewing-assignment deletion with the same locked output/report evidence as return, blocks progressed/historical deletion, and catches direct record links despite mismatched line names. Fifty-eight focused sewing cases and Ruff pass. Other alternate-writer rules remain.
- **DB01 partial follow-up:** `794f213` requires catalog item units for stock-backed accessory issues while leaving manual custom-unit issues intact. Twenty-nine focused/adjacent inventory cases pass, including rollback when a later line has an invalid unit. Other alternate-writer rules remain.
- **DB03 partial follow-up:** `7453b9b` bounds new role/user/factory-policy permission JSON grant vectors at 150 entries and 128 characters each, without breaking reads of oversized legacy rows. Fifty-one adjacent backend cases and Ruff pass; other editable JSON remains.
- **DB02 partial follow-up:** `8711c96` types purchase-request creation status as `draft`/`pending_approval` while retaining legacy normalization. Forty-one adjacent tests and Ruff pass; other loose fields remain.
- **DB01/DB02/DB03 partial follow-up (2026-09-25):** `9404a6fe` blocks a mismatched replacement Cutting batch with an existing reservation (22 material-reservation tests); `8a874361` bounds sewing-correction size JSON before writes (22 correction tests); `9bdf79c4` blocks generic work-order status transitions without mutating notes/audit while retaining unchanged-status metadata edits (7 focused and 3 adjacent tests). Ruff and diff checks pass. Broader alternate-writer, command and editable-JSON reviews remain; official counts do not change.
- **DB01/DB03 partial follow-up (2026-09-25):** `f224b6f5` validates the legacy Cutting passport primary material's batch/catalog/planned unit before synthesizing the row (26 reservation cases); `2b79edd3` bounds accessory costing prices and rejects oversize edits without changing stored JSON or audit (9 focused/workflow cases). Ruff passes. Historical rows remain untouched and both findings stay partial.
- **DB01 partial follow-up (2026-09-25):** `efbae11f` enforces the item catalog unit on new item-linked forecast recommendations; omitted units become the catalog unit and mismatch/missing-reference failures write no recommendation or audit. The full 22-case forecasting suite and Ruff pass. Existing mismatches remain for business review; other DB01 writers remain.
- **DB02/DB03 payroll partial follow-up (2026-09-25):** `cb0af8e3` types adjustment kind as Bonus/Deduction while preserving existing normalization and sign defaults. `4f4efcb8`/`3a513e93` bound open raw employee/work snapshots to 16 KiB of compact UTF-8 JSON and 16 levels both before and after QR enrichment; all six focused boundary, compatibility and no-write tests pass. `063d76ac` shortens generated payroll-test role names to meet the existing 64-character limit. The combined eight-module payroll selection passes 88; Ruff and diff checks pass. Other command/JSON gaps remain.
- **DB01/API05 partial follow-up (2026-09-25):** `a229d40d` enforces catalog, batch and explicit material unit agreement when creating production orders; the full material-reservation module passes 31 and Ruff passes. `0e9bb6ba` wires effective forecasting.view factory grants into the branded-suggestions route; secondary grants, Super Admin and unassigned-model exclusion are covered. `5709af3c` aligns a recommendation text-bound fixture to the existing item-unit rule. Sixty forecasting/traceability tests and Ruff pass on the working branch. Dashboard, item-reorder and recommendation reads/writes remain unscoped pending policy for unattributed item/inventory data; API05/DB01 stay partial.
- **DB03 item composition partial follow-up (2026-09-25):** `de3bda68` bounds newly saved or changed inventory Item composition JSON to 100 nonblank rows and 255 characters per stripped name; blank rows stay filtered, existing over-total precedence remains, and unchanged legacy JSON (including extra keys) can survive an unrelated PATCH. The new boundary module and two adjacent inventory modules pass 54 on the working branch; Ruff passes. Other editable JSON remains.
- **DB02 quality severity partial follow-up (2026-09-25):** `a9bce608` constrains quality-check creation severity to low/medium/high/critical with low as the default. Unsupported values fail with 422 and no QualityCheck/audit write; authentication precedence and all four accepted codes have API regressions. Three adjacent quality modules pass 24 on the working branch; Ruff passes. Other command vocabularies remain.
- **DB01 ModelBOM unit partial follow-up (2026-09-25):** `eedfe49b` enforces item-linked BOM unit agreement with catalog Item.unit (and linked batch unit), including unit-only PATCH; itemless Usluga fabric descriptions and unchanged legacy mismatches remain editable. `e860d79e` corrects a clone-model fixture that had hard-coded a noncatalog unit. Numeric/reference precedence and no-write failures have regressions; the catalog numeric, catalog and Usluga selection passes 67 on the working branch, and Ruff passes. Historical mismatches/other writers remain.
- **DB03 ProductionOrder attachment partial follow-up (2026-09-25):** `7767ee3c` bounds new/changed printing attachment metadata to 50 safe uploaded paths with URL/name/type lengths across direct and planning creation plus PATCH. Unchanged legacy JSON/unknown keys remain readable and survive unrelated edits. `aa7f27c2` preserves missing/closed planning-order error precedence before the attachment check while still rejecting invalid new-order attachments before a planning-order flush. Sixty-four combined attachment, update, upload-lifecycle and signed-flow cases plus Ruff pass; other editable JSON remains.
- **DB01 clone/DB03 HR skills partial follow-up (2026-09-25):** `6779a1e2` preflights source BOM catalog/batch/unit agreement before cloning a Model, rejecting legacy mismatches without new Model/BOM/audit writes while preserving itemless Usluga descriptive rows; four focused clone cases and Ruff pass. `941c8cf8` bounds new/changed HR position required-skills lists at 50 strings of 160 characters, preserving unchanged oversized legacy lists on full PATCH and reads; 15 adjacent HR position cases and Ruff pass. Broader writer/JSON findings remain partial.
- **DB05/OPS06 partial:** `d23738c` previews revision0130's targeted permission change without writes, including exact before/after snapshots and malformed-input blockers; 4 focused cases pass. `13f5994` hashes a paired database dump/storage snapshot, verifies exact files and refuses manifest overwrite; 7 local cases pass with 3 Windows symlink-privilege skips. Neither tool replaces operator row review, an approved migration plan, off-host storage, an actual restore drill or RTO/RPO evidence.
- **FN07/DB01/DB03 partial follow-up:** `f7ff339` rejects sub-cent 1C invoice/payment amounts while keeping NaN/Inf per-row isolation; 18 focused cases pass. `287fab2` aligns inventory warehouse/unit/reference writer rules; 72 focused cases pass. `1610d50` enforces positive sewing assignment quantity and completed≤assigned across writers/resizing; 40 focused/adjacent cases pass. `4def8fb` validates established model-detail JSON containers while preserving legacy unknown keys; 5 focused cases pass. Other alternate-writer, numeric and JSON-shape gaps remain.
- **PERF08/PERF22 fixed:** `330374c` returns a minimal receiving queue in 50-row default/100-row maximum pages, with detail by package ID unchanged; 34 focused backend tests pass. `cadb035` defaults Usluga orders to bounded 100-row pages and keeps frontend load-more; 27 backend tests and the Bun component harness pass.
- **FN08/OPS02 partial:** `285b69d` excludes cancelled invoices from dashboard/monthly revenue, but valuation/currency rules remain owner policy. `50a050d` bounds configured backend worker pools to 48 maximum two-slot connections against the documented 100, but other clients and load need verification.
- **DB08 fixed:** `1141d40` aligns all30 formerly unresolved ORM-created schema contracts and ten equivalent index names with migration-created PostgreSQL without a migration or data rewrite. Remaining normalized differences are the exact reviewed migration-owned indexes/default representations/unique constraints, plus separately validated generated columns. Local migration contract passes14 with2 expected skips, PostgreSQL passes16 and124 compatibility cases pass; independent review repeats14/2 and all16 PostgreSQL cases.
- **DB06 partial:** `a590222` prevents fresh migrations from duplicating five semantic foreign keys and removes redundant unique-index creation for branded-planning and material-reservation numbers. The migration contract now rejects duplicate FK signatures and duplicate constraint/index uniqueness definitions; local tests pass14 with2 expected skips and all16 disposable-PostgreSQL migration cases pass. Existing deployments and the173 historical index candidates require measured plans and explicit migration approval.
- **SEC09 fixed:** `f35a62e` requires exact proxy CIDRs in hosted startup, shares trusted client/scheme resolution across rate limits/CSRF/HSTS/cookies, bounds backend public-auth and frontend reset-proxy bodies, and serializes every credential rotation with session cutoff, reset-link invalidation and metadata-only audit. The broad backend gate passes142 with7 expected skips, targeted PostgreSQL races pass4, reset proxies pass20, and independent review reruns the focused backend selection, all15 password/reset PostgreSQL cases and all20 proxy cases successfully. Activation requires exact `TRUSTED_PROXY_CIDRS` plus `FORWARDED_ALLOW_IPS=`.
- **PERF02 fixed:** `90b7f21` moves exact accessory queue derivation, total and global shortage/partial/ready ordering into one PostgreSQL statement before paging. At1/50/401 candidates it stays one round trip; the401-row plan executes in23.668ms with expected indexes. Duplicate BOM, decimal/epsilon, blank-unit, returns, aliases, completion/search, empty-page and auth parity pass7 disposable-PG plus14 independent SQLite cases.
- **AT06 fixed:** `135d669` versions connector roster/event snapshots before device reads and advances atomic per-device checkpoints in the existing settings table. Late older rosters cannot regress presence; stale/unversioned events append unique history without replacing newer metadata; legacy compatibility remains until version adoption. Focused attendance32, connector44 and disposable-PG8 pass; independent reruns confirm6 source-version,44 connector and8 PG cases.
- **PERF36 fixed:** `bd36441` caps task broadcasts before policy iteration or writes and replaces full User hydration with one cap+1 authorization projection. The 1/50-recipient path stays at4/4 SQLite or6/6 PostgreSQL SELECTs; 401 recipients read only251 target rows and produce no task, notification or audit rows. API02 reference/factory/package/denial behavior remains covered; the combined local selection passes80 with10 expected PG skips, all17 disposable-PG combined cases pass and the independent broadcast rerun passes8.
- **API02 fixed:** `22593e7`, `31350dc`, `73d7ca4` validate task commands, lock supported reference targets through commit, and mirror each target read route's permission/factory policy for actors and recipients. Legacy aliases/orphans remain compatible; explicit denials, secondary grants, Super Admin factory switching, unchanged-reference reassignment, broadcasts and no-write failures are covered. The focused selection passes79 with4 expected local PostgreSQL skips; all15 disposable-PostgreSQL cases pass, including a verified lock wait via `pg_stat_activity`/`pg_blocking_pids`. Referenced broadcasts stay within8 SELECTs at1/50 recipients; later PERF36 work bounds and narrows recipient discovery.
- **PERF32 fixed:** `ed4be77` replaces implicit inbox/forecast object graphs with narrow,400-ID reference maps. Material payload, planned demand, production-context, branded-analysis and dashboard counts remain bounded at4/4/8,4/4/7,6/6/6,7/7/10 and15/15/18 for1/50/401;44 focused/adjacent cases pass without hidden BLOB/JSON/attachment/roll hydration.
- **PERF38 fixed:** `a8b1707` uses canonical signed-user rate buckets on normal routes while preserving IP budgets for anonymous, invalid/expired-token and public login/reset traffic. Sequential and concurrent same-office-IP isolation, route-auth authority, proxy trust, off-loop SQLite work, fixed-window cleanup and atomic Redis TTL initialization pass25 final focused cases; the preceding combined auth/security selection passes29.
- **PERF12 fixed:** `6277260` batches print-run package context and bundle-sheet canonical QR references. Print-run reads fall14/504/2004→14/14/14 and bundle reads8/57/207→7/7/7 at1/50/200; package sheets stay bounded through500. Twenty-nine focused and12 compatibility cases pass.
- **PERF19 fixed:** `cf7e606` shares one scoped evidence UNION across bundle-edit guards and filters every grouped reconciliation aggregate to the actual scope set. Full endpoint SELECTs fall41/41/41→29/29/29 at1/50/401;28 focused and one real-PostgreSQL lock/rollback case pass.
- **PERF10 fixed:** `b6ab791` batches full print-run receive reads behind one locked gate while preserving manifest order, rollback and receiver serialization. At1/50/401 packages, SELECTs fall21/756/6021→18/18/31;23 focused SQLite and3 real-PostgreSQL concurrency/rollback cases pass.
- **PERF20 fixed:** `05206df` batches cutting reservation/stock locks, bundle departments/numbers, Usluga scan logs and packaging BOM stock reads. Full1/50/401 endpoint counts are41/41/41,30/30/30,27/27/27 and18/18/18;32 focused,36 adjacent inventory,120 broad compatibility and12 real-PostgreSQL cases pass.
- **OPS08 partial:** `06705b9` removes python-jose/ecdsa, adopts PyJWT2.14.0 and Node24 LTS, and adds dependency/runtime contracts. Focused19 and broad85 security cases plus package audits pass. Built-container OS scanning still requires Docker/Trivy or an authorized CI/release run.
- **PERF14 fixed:** `10198ea` batches sorted material item locks, reservation-number allocation and reservation/material-plan/audit transfers while preserving input-order validation, rollback reuse and concurrency order. The focused local selection passes14 with17 PostgreSQL-only skips; all28 disposable-PostgreSQL query-growth, plan, concurrency and rollback cases pass.
- **PERF09 fixed:** `a93409d` completes transaction-local package-write reuse and batches number ranges, owners, recipients and print children. The34-case focused and34-case adjacent selections pass;2 disposable-PostgreSQL races prove scalar/bulk row→number lock order and rollback range reuse.
- **PERF18 fixed:** `96d7a61` prevents PostgreSQL from inlining and recomputing grouped packaging target maps per sewing scope. The25-case focused selection and all6 disposable-PostgreSQL1/50/401 plans pass. The adjacent selection has28 passes and3 unrelated overlong-role fixture failures before receive-options execution.
- **PERF26 fixed:** `fbdda65` batches legacy Finished Goods metadata repair across sales-order/model and collection references. The 1/50/401 distinct-reference measurements are1/1/2 sales-item reads,1/1/2 collection reads and one stock read; reservation candidates remain bounded at3/3/5 total and2/2/4 stock SELECTs. The focused suite passes15 and the adjacent branded-stock/Finished Goods selection passes99. Per-row updates, reservations and response output are necessary O(N) work.
- **PERF35 partial:** recruitment, employee/role/data-table directories, global search, dashboard production, shipment customers/eligible orders/ready packages, branded planning/stock, bundle history, package changes/storage search, material reservations, model BOM items/collection seasons, packaging receipts, waste, sewing flows/reports/assignments/history/utilization, customer order/payment history, work/print/purchase orders, notifications, brands, quality checks, finance/payroll lists, cutting passports, HR positions, Finished Goods, suppliers, departments, warehouses, forecasting, tasks, inventory colors and cutting operators offer opt-in bounded pages while preserving legacy shapes. `d2a65b9` also streams payroll summary input rows in400-row batches. Independent PostgreSQL paging evidence passes20. Complete storage/inbox/HR/payroll graphs and grouped response contracts still need owner-approved lazy loading.
- **OPS09 partial:** `aa8fac1` uses per-request nonces for frontend executable scripts and adds frontend HSTS; the executable header contract and targeted lint pass. `c99e609` provides validated per-client 1C rotation slots and client-scoped idempotency locking; `f961a65c` wires the finance sync route to those helpers. SEC09 covers backend proxy/HSTS; the latest local review rejects trust-all and noncanonical proxy CIDRs. The combined SEC09/OPS09 backend selection passes38 with1 expected PostgreSQL-only skip, and Ruff passes. Production proxy/header/credential values, caller rollout, and a policy for globally shared 1C external IDs remain unverified, so OPS09 is partial.
- **PERF21 fixed:** `873979a` scopes production-batch work orders and package histories in SQL with exact legacy fallback. Production-batch graphs stay17/17/17 SELECTs and populated package histories21/21/21 at1/50/401;63 focused cases pass with one PG-only skip and the real PostgreSQL17 plan passes.
- **PERF33 fixed:** `31b4254` retains paged/streamed stocktake reads inside one PostgreSQL read-only repeatable-read snapshot. The69-case compatibility selection and3 real-PostgreSQL boundary/cleanup races pass; changed-detail query counts are9/10/14 at1/50/401.
- **DB02/DB03 partial:** finite write vocabularies cover HR status, inventory/purchase QC, 1C invoice status, manual payment methods, sewing assignments, sales-line sources, package types, attendance-device vendors and default language; model paid-operation, HR profile and model-size measurement JSON are shape-checked. Invalid requests preserve authorization/resource/conflict precedence and have no business/audit side effects; legacy JSON reads remain compatible.
- **PERF39 fixed:** `534c8bf` gates inactive/unauthorized directories and removes redundant customer requests. `b669bd4` adds authorization-equivalent page-context projections, collapsing Production Order→model2→1, authorized Sales Order→requirements2→1 and Work Order→Production Order→Sales Order/model4→1 browser requests; measured SQL falls21→15,10→8 and30→20. Three focused backend cases, full frontend stabilization, strict TypeScript, targeted ESLint, Ruff and diff checks pass.
- **UI03 fixed:** `3532e8e` extends same-lock reconciliation to manual receipts, print runs, package creation, manual shipments and purchase receipts. Recovery sends one status request and zero repeated business writes; unused keys are tombstoned, deleted/revoked results expose no stored response, and cross-tab browser evidence reuses one key. Twenty-six focused local cases, seven real-PostgreSQL races, all frontend recovery/stabilization/type/lint gates, Ruff and diff checks pass.
- **OPS05 partial:** `c275be7` makes `/ready` cover PostgreSQL plus the required shared store within one bounded concurrent budget, with generic status, single-flight timeout guards and read-only Redis/SQLite/in-memory pings; `/health` stays liveness. Candidate/stable/HAProxy config and postflight use `/ready`; the release observer checks internal and public `/ready` throughout its 10/30-minute window in addition to liveness/login. Releasing/applying the config and verifying active/public dependency health remain infrastructure work.
- **WF04 fixed:** Packaging, Printing and Cutting evidence remains green. Sewing enforces int4/nonnegative bounds, passed<=sewn and processed<=explicit input while preserving failed input beside good sewn output and input_qty=0 inference; invalid requests have no business/audit side effects. The corrected combined slice passes33, and the four full-suite compatibility cases pass within a24-test selection.
- **PERF10 then partial:** earlier placement work reduced source-type SELECTs at1/50/401 packages from **12/61/422 to12/12/23**. The later `b6ab791` print-run closure is recorded above.
- **PERF30 fixed:** `b4db4b4` preserves clone numbering while using exact bounded code probes; family rename and approval remain bounded. The 66-case compatibility selection and6 disposable-PostgreSQL plan/concurrency cases pass. Covered clone and family queries use their indexes without sequential scans; colliding namespace allocation serializes and rollback releases the lock.
- **PERF39 fixed (earlier model-detail slice):** model-detail seasons and BOM contracts pass. BOM metadata is skipped on hidden tabs and loaded for read/edit on the visible Materials tab. CI lint, strict types and optimized build pass in `35675969462`; the remaining detail-page waterfalls are closed by `b669bd4` above.
- **PERF06 fixed:** bulk input is capped at500; references/duplicates are batched, automatic period matching is O((rows + periods) log periods), and the finalized audit-chain path reads one head per transaction. At1/50/401 records audit SELECTs remain1 and other SELECTs stay below30.
- **PERF40 partial:** `b607eda` completes all11 inventoried application upload handlers: one shared process-local limiter bounds buffering/conversion/writes, blocking SQL runs in fresh worker-owned sessions, scope/row state is rechecked under locks, and cancellation waits for transaction/file ownership before cleanup. Consolidated tests pass60 with4 expected skips; all9 PostgreSQL attendance races pass, while independent review repeats16 focused and all9 PostgreSQL cases. Only global multi-process admission/topology and pre-handler multipart spooling remain external.
- **DB01 partial:** `3d15874` gives purchase receipts the shared item-category warehouse rule before any business/audit/idempotency write while preserving auth/reference precedence and batch/movement parity. Twelve focused cases pass independently; the broader69-case selection has3 expected PostgreSQL skips and query growth remains4/4/8 at1/50/401. Later inventory writers align warehouse/unit/reference rules and sewing writers align progress bounds; `df14ad6` blocks deletion of progressed/historical assignments and strengthens linked-record return checks with58 focused passes. Other alternate writer/reference rules remain.
- **DB01 partial, stock consumption:** `5b4ac7c` rejects mismatched catalog/reservation/batch units before stock mutation and uses the catalog unit for batchless ledger entries. The seven-module adjacent selection passes73 and Ruff passes. No unit conversion is defined; historical mismatches require data correction.
- **PERF35 partial, inbox queues:** `bfb51a9` bounds the replacement-cutting screen to 50 rows with exact totals and global suppression of affected ordinary work orders; `3e6c5d4` bounds Awaiting Packaging to 50 rows and shows one row per production batch, including aggregate unbatched work. The combined 9-test selection passes; strict TypeScript, targeted ESLint and Ruff pass. Legacy unpaged API paths remain compatible. Broader inbox graphs and performance under production load remain unverified.
- **PERF35 partial, core department inbox (2026-09-25):** `2b2ec74c..e2b5067e` adds exact canonical identity pages with page-only card hydration and a default-compatible skip-core switch for other widgets; `6c412e44` makes non-Cutting screens fetch 50 rows with Load more. Thirty-five adjacent backend, two explicit auth/factory and four production-flow inbox cases pass, along with Ruff, strict TypeScript, targeted lint, the screen contract and full frontend stabilization. Exact totals still require an O(N) scalar identity scan/index; FGS ready-to-ship and remaining graphs/load evidence stay open.
- **PERF35 partial, accessory Receive/Return picker (2026-09-25):** replaced the accessory inventory receive screen's capped `page_size=500` production-order option fetch with server-searched 50-row pages, exact filtered totals and Load more. Existing production-read endpoint authorization is unchanged; chosen and deep-linked orders remain selected outside the visible search page. Five production-order pagination/search/auth/parity backend tests, the focused frontend contract, strict TypeScript, touched-file ESLint, Ruff and diff checks pass. The stale HR Employees position harness and two renamed script paths were corrected; the full frontend stabilization suite now passes. Other eager receiving summaries and PERF35 graphs remain; no finding status/count change.
- **Committed continuation through `f378278` (range `78155f0..f378278`):** follow-up purchasing/finance/inventory/payroll/production/catalog/HR/stocktake/package/sewing finite-value and storage bounds have focused regression modules; purchasing quantities, order/receipt quantities and costs retain established validation precedence. PERF35 adds fabric-scan summary and list/export paging. PERF40 adds route lifecycle regressions for HR documents, model images, production/sales attachments and attendance photos. PERF12 package labels accept 500 and reject 501 before package SELECT/render (focused module: 8 passed). PERF33 adds stocktake ID/undo bounds on top of paged detail, streamed CSV and bounded scan evidence. Image BLOB projection, attendance aggregation and traceability pushdown are also committed; remaining scope is unchanged and listed above. This entry covers committed history only, not dirty working-tree changes.
- **Committed continuation through `95111b5` (range `f378278..95111b5`):** `3e50527`, `50cbbe8`, `41700e6`, `e27c514`, `9d368e9` and `ee75b88` add HR/payroll/cutting/stock-batch/receipt numeric bounds; `2f92465` projects bundle label images; `403af90` reuses traceability warehouse references; `bc3e63f` removes an unused Process Tracking export BOM load; `263d2b5` and `95111b5` reduce stocktake detail/summary hydration; `623c169`/`b2958f0` bound attendance photo concurrency/buffering. See focused modules named in [audit](../audit_ismail.md). Inventory colors now deduplicate in SQL and several ID/reference inputs are range-checked. This is committed-source evidence only, not a full-suite/CI result; official counts remain 79 fixed/33 partial/15 open/48 remaining, and uncommitted changes/API02 scaffold are excluded.
- **Closure pass through `44faa28`:** PERF13 and PERF28 were fixed with the constant/chunk-bounded query evidence above. At that point PERF02, PERF08, PERF22 and PERF33 remained partial; `31b4254` subsequently closes PERF33 with repeatable-read snapshot races. Stocktake bounded-memory suites passed63 at the earlier checkpoint; receiving queue suites47; Usluga backend/frontend compatibility24 plus the executable load-more test; accessory queue/planning23. This is focused evidence, not a full-suite/CI claim.
- **SEC08 fixed:** `e7782ec` restricts raw PATCH to audited `departments.name`; `297d808` names the repair and `df1d08f` adds approved local-only revision0132 plus named Super Admin deactivation. Generic hard delete stays blocked. Independent backend, migration, frontend-handler and strict-TypeScript gates pass. No production migration was applied.

- **FN01:** `test_1c_sync_row_isolation.py` + FN03 → 25 passed including 8 PostgreSQL cases. Import good/bad/good rows: valid rows commit, failed row leaves no partial changes; caller rollback removes accepted rows too. Dialect assertions guard against accidentally testing SQLite as PostgreSQL. O(n) savepoints/writes; no speed claim.

- **FN03:** `test_1c_payment_reassignment.py` → 21 passed (6 PostgreSQL). Move payment A→B: A loses its credit, B gains it; amount edits, retries and rollback preserve totals. Concurrent moves/manual payments serialize. Run with the disposable-cluster command above. Added lock reads are bounded per batch; processing/status aggregation still grows with payment rows—no O(1) claim.
- **FN05:** `test_customer_payment_cents.py` → submit 0.01 advance or 1.01 against a 1.00 invoice; residual credit is retained. Sub-cent, negative and nonfinite amounts reject before writes. Existing invoice settlement tolerance is unchanged.
- **SEC11:** `test_quality_check_factory_scope.py` + `test_production_flow.py` → 93 passed. POST `/api/quality/checks`: six wrong-factory pairs return 403 with no quality/audit inserts; own/explicitly selected factory works, missing work order returns 404. One added department lookup; no throughput claim or visual UI proof.
- **OPS02 partial:** `test_db_pool_configuration.py` → 9 passed without a PostgreSQL connection. Explicit 8/4 and zero-overflow honored; unset values retain 5/10; SQLite unaffected. Maximum configured connections must still be budgeted across every process/slot/client.
- **PERF05:** issuance/list/payroll suites → 64 passed. POST `/api/payroll/qr-labels/issue`: repeated new UID returns issued=2/created=1/existing=1 and one audited creation. Existing ambiguous retries succeed; new ambiguous references reject; wrong-factory references reject without insert; canonical aliases preserve UID/money/manual text. Reference-only SELECTs **8/44/204 → 7/7/7** at 1/10/50; no-reference 4/4/4, retries 3/3/3; 401-reference chunk test passes. Lookup round trips grow by 400-value chunks; writes/output O(n), reference preparation includes sorting. Not O(1) total work or peak-load proof.
- **API02 fixed:** `test_task_reference_factory_scope.py`, `test_task_input_validation.py`, `test_task_assignment_authorization.py`, broadcast and notification contracts →79 passed/4 PostgreSQL-only skipped locally;15 passed on disposable PostgreSQL. POST/PATCH `/api/tasks` rejects invalid states/dates/reference pairs, missing or unauthorized targets and inaccessible recipients without task/notification/audit writes. Supported target rows are locked through commit; raw aliases and legacy orphan edits/reassignment remain compatible. Reference authorization adds a bounded number of reads; referenced broadcasts stay within8 SELECTs at1/50 recipients.
- **Scoped API observation:** quality/task/payment regression batch → 83 passed, 6 PostgreSQL-only skipped; 114 requests across 7 routes, no failed tests. This is a dirty-worktree batch, not updated full-suite coverage. Separate PostgreSQL results above remain required.
- **DB03 partial follow-up (2026-09-25):** `4df74a3f` bounds changed HR employee profile JSON writes to 16 KiB UTF-8 and 16 nested levels after reference/conflict checks, preserving exact unchanged oversized/deep legacy JSON; `68a62230` caps raw cutting bundle-plan rows at 1,000, rejects booleans/fractional counts or quantities, and validates color/size against Bundle column widths. HR profile validation passes 20 (six-module HR/employee selection: 36); cutting quantity validation passes 68; adjacent production-flow, Usluga and cutting-material bound selection passes 115. Ruff, Python compile and diff checks pass. Flexible Model.details_json extensions remain unbounded, so DB03 remains partial.

- **DB03 Model.details_json bounds (2026-09-25):** `fea4fb18` limits changed documents to 64 KiB compact UTF-8 and 16 nested containers, rejects non-finite/non-JSON values, and preserves exact unchanged legacy documents through iterative comparison. Create, full/paid-operation PATCH, model clone, variant create/edit, and family identity renames are guarded before persistence; clone/rename paths are checked before deep-copying. The focused model-details, costing, catalog/clone/variant, and rename selection passes 77 tests; Ruff and diff checks pass. Other editable JSON and offline import/migration paths remain outside this slice; DB03 remains partial.
- **DB03 SystemSetting JSON bounds (2026-09-25):** `e798c94a` bounds changed shared settings sections to 16 KiB compact UTF-8 and 16 nested containers, preserving semantically unchanged legacy field values. The settings PATCH and company-logo replacement paths validate before database/audit persistence. The focused settings, authorization, logo-upload and projection selection passes 51 with four expected PostgreSQL-only skips; Ruff and diff checks pass. HR settings, attendance checkpoints, and other writable JSON remain outside this slice; DB03 remains partial.
- **DB03 HR settings and attendance checkpoints (2026-09-25):** `a52dc5fc` bounds changed factory HR settings and attendance source-checkpoint documents to 16 KiB compact UTF-8 and 16 nested containers, while exact unchanged oversized/deep legacy values remain round-trippable. HR guard follows weekend-day validation; attendance guard is centralized at its checkpoint writer. The focused HR/attendance settings, workspace, checkpoint, import and vendor selection passes 48 tests; Ruff and diff checks pass. Other editable JSON and offline import/migration paths remain; DB03 remains partial.

### Final acceptance

- Run the full suite and all opt-in PostgreSQL cases on the final commit; preserve failures and skips.
- Check success, validation, role/factory denial, missing records and database side effects per changed API. For mutations also check retries, rollback and concurrent writers.
- Use the API ledger to find unexercised operations; a 2xx hit alone is not correctness coverage.
- Test realistic data volumes and an agreed concurrent-user workload on isolated staging. Record p50/p95/p99, errors, SQL counts, CPU/RAM and pool waits.
- Owner-approved policies, device firmware checks, historical-data repair and backup restoration require separate evidence. No production-readiness sign-off until these gates pass.
